import React from 'react';
import Header from '../components/header';
import { AppBar } from '@material-ui/core';

const Main = () => {
	return (
		<AppBar>
			<Header />
		</AppBar>
	);
};

export default Main;
